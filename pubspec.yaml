name: sqlbrite
description: sqlbrite for flutter- A lightweight wrapper around sqflite which introduces reactive stream semantics to SQL operations.
version: 1.1.0
author: Petrus Nguyễn Thái Học <hoc081098@gmail.com>
homepage: https://github.com/hoc081098/sqlbrite.git

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.22.0
  sqflite: ^1.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.0.0
  test_api: ^0.2.4

flutter:
